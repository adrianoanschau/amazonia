{
  "name": "amazonia-api",
  "version": "1.0.0",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "start:dev": "npx nodemon",
    "start": "yarn build && node build/index.js",
    "build": "rimraf ./build && tsc"
  },
  "devDependencies": {
    "@types/node": "^20.1.0",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
